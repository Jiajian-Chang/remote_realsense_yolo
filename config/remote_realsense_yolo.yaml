# Configuration file for Remote RealSense YOLO node

remote_realsense_yolo_node:
  ros__parameters:
    # YOLO model parameters
    yolo_model: "yolo11n.pt"  # Ultralytics YOLO model
    yolo_model_cache_dir: "~/.cache/ultralytics"  # Model cache directory
    yolo_confidence: 0.7      # Detection confidence threshold
    yolo_iou_threshold: 0.4   # IoU threshold for NMS
    detect_person_only: true  # Focus on person detection
    
    # Camera parameters
    image_topic: "/remote/d455/color/image_raw/compressed"
    use_compressed: true
    
    # QoS parameters for compressed image
    qos_reliability: "reliable"  # reliable or best_effort
    qos_durability: "transient_local"  # volatile or transient_local
    qos_depth: 10
    
    # Display parameters
    show_image_window: true
    window_name: "Remote RealSense YOLO"
    window_fullscreen: false
    window_resizable: true
